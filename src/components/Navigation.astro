---
const routes = [
  {
    name: 'Home',
    path: '/',
  },
  {
    name: 'About',
    path: '/about',
  },
  {
    name: 'Blog',
    path: '/blog',
  },
  {
    name: 'Contact',
    path: '/contact',
  },
];

const currentRoute = Astro.url.pathname;
---

<nav>
  {
    routes.map(({ name, path }) => (
      <a href={path} class:list={[{ selected: currentRoute === path }]}>
        {name}
      </a>
    ))
  }
</nav>
